apiVersion: v1
kind: Secret
metadata:
  name: qbittorrent-secret
  namespace: qbittorrent-operator
type: Opaque
data:
  # Base64 encoded values - these are examples, replace with actual values
  # echo -n "admin" | base64
  username: YWRtaW4=
  # echo -n "adminadmin" | base64  
  password: YWRtaW5hZG1pbg==
